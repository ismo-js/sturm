export class Observable {
    static of(...a) {
        return Observable.from(a)
    }

    static from(a) {
        return new Observable()
    }

    constructor(src) {

    }

    chain(a) {

    }

    subscribe(observer) {
    
    }
}
